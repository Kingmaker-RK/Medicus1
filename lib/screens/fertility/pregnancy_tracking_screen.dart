import 'package:flutter/material.dart';

class PregnancyTrackingScreen extends StatelessWidget {
  const PregnancyTrackingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pregnancy Tracking')),
      body: const Center(child: Text('Pregnancy Tracking Content Here')),
    );
  }
}
