import 'package:flutter/material.dart';

class NewbornChildCareScreen extends StatelessWidget {
  const NewbornChildCareScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Newborn & Child Care')),
      body: const Center(child: Text('Newborn & Child Care Content Here')),
    );
  }
}
