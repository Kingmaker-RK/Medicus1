import 'package:flutter/material.dart';

class PregnancyPlanningScreen extends StatelessWidget {
  const PregnancyPlanningScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pregnancy Planning')),
      body: const Center(child: Text('Pregnancy Planning Tools Here')),
    );
  }
}
